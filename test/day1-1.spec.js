import { matchingDigitsSum } from '../src/day1-1';

describe('Sum of matching digits', () => {
	it('Exists', () => {
		expect(true).toBeTruthy();
		expect(matchingDigitsSum).toEqual(jasmine.anything());
	});
});
